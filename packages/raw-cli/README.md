# raw-cli

CLI tool for scaffolding RAW projects.

## Usage

```bash
npx raw-cli new my-project
```

This will create a new RAW project with:
- Vite configuration
- Example counter app
- All necessary dependencies

